const students = ["Ana", "Brunto", "Carla", "Diego", "Eduarda", "Felipe", "Gustavo"]

for (let i = 0; i < students.length; i++) {
    console.log(`${i+1} - ${students[i]} ${Sutendts[i] == "Felipe" ?"(Ausente)" ? }`)
}
